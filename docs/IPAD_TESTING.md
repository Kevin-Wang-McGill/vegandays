# iPad 适配测试指南

本文档说明如何在本地验证 iPad 布局适配，确保所有页面在 iPad 上正常显示。

## 📱 目标设备尺寸

### iPad 设备参考尺寸（逻辑像素）

| 设备 | 竖屏尺寸 | 横屏尺寸 | shortestSide |
|------|---------|---------|--------------|
| iPad Air 11" | 820 × 1180 | 1180 × 820 | 820 |
| iPad Pro 11" | 834 × 1194 | 1194 × 834 | 834 |
| iPad Pro 12.9" | 1024 × 1366 | 1366 × 1024 | 1024 |
| iPad mini 6 | 744 × 1133 | 1133 × 744 | 744 |

> **Tablet 判定规则**: `shortestSide >= 600` 被判定为 Tablet

## 🔧 本地验证方法

### 方法 1: iOS 模拟器

```bash
# 列出可用的 iPad 模拟器
xcrun simctl list devices | grep iPad

# 启动特定 iPad 模拟器
open -a Simulator
# 然后在 Simulator 菜单中选择 File > Open Simulator > iOS > iPad Air 11-inch

# 运行应用
flutter run
```

**推荐测试设备**:
- iPad Air (11-inch) (5th generation) - 主要测试目标
- iPad Pro (11-inch) (4th generation) - 验证 Pro 布局
- iPad Pro (12.9-inch) (6th generation) - 大屏极端情况

### 方法 2: Debug 设备信息面板

在 **Debug 模式** 下，进入 **Settings 页面**，滚动到底部可以看到 **"Debug: Device Info"** 面板，显示：

- 当前屏幕尺寸
- shortestSide 值
- 设备类型（Phone/Tablet）
- Safe Area Insets
- Text Scale Factor
- Responsive Scale Factor

### 方法 3: Device Preview 模式（Debug Only）

在 Debug 模式下，屏幕右下角会显示一个 **黄色 FAB 按钮** (📱 图标)：

1. 点击该按钮进入 Device Preview 模式
2. 使用顶部工具栏切换不同设备尺寸
3. 点击旋转图标切换竖屏/横屏
4. 点击 X 退出预览模式

### 方法 4: Widget Tests

```bash
# 运行 iPad 布局测试
flutter test test/ipad_layout_test.dart

# 运行所有测试
flutter test
```

测试覆盖：
- iPad Air 11" / Pro 11" / Pro 12.9" 竖屏和横屏
- textScaleFactor 1.2 和 1.4 的大字体模式
- 主要页面：HomeScreen、SettingsScreen、Onboarding 流程

## ✅ 检查清单

### P0 - 必须通过

| 检查项 | 如何验证 | 预期结果 |
|-------|---------|---------|
| 底部按钮可见 | 查看 HomeScreen 底部 "Check in" 按钮 | 完全可见，不被 NavigationBar 或 SafeArea 遮挡 |
| 底部按钮可点击 | 点击 "Check in" 按钮 | 触发正常响应，无死区 |
| 文案不溢出 | 检查所有标题和说明文字 | 无黄黑条纹警告，文字完整显示 |
| BottomSheet 可见 | 点击动物面板中的动物 | 弹窗完整显示，"Bring home" 按钮可见可点 |
| 滚动正常 | Settings 页面滚动 | 可以滚动到底部，内容完整 |

### P1 - 应该通过

| 检查项 | 如何验证 | 预期结果 |
|-------|---------|---------|
| 内容居中 | 查看 Settings、Onboarding 页面 | 内容在大屏上居中显示，不过度拉伸 |
| 按钮尺寸适当 | 查看各页面按钮 | 按钮高度和字号在 iPad 上适当放大 |
| 间距舒适 | 整体视觉检查 | 内容不过于紧凑，留白适当 |
| 动物面板宽度 | 展开右侧动物面板 | 面板宽度适中（~320px），不过窄 |

### P2 - 建议检查

| 检查项 | 如何验证 | 预期结果 |
|-------|---------|---------|
| 横屏布局 | 旋转 iPad 到横屏 | 无 overflow，内容仍居中 |
| 大字体模式 | 设置 > 辅助功能 > 字体大小 调大 | 布局仍正常，无溢出 |
| SafeArea 处理 | 检查刘海/圆角区域 | 内容不被遮挡 |

## 🔍 常见问题排查

### 问题 1: 底部按钮被遮挡
- 检查是否使用了 `SafeArea`
- 检查 `bottom` 定位是否考虑了 `MediaQuery.of(context).padding.bottom`

### 问题 2: 黄黑条纹溢出警告
- 检查 `Row` 中的 `Text` 是否使用了 `Expanded` 或 `Flexible`
- 检查是否有固定宽度的元素超出屏幕

### 问题 3: 内容过度拉伸
- 检查是否使用了 `ConstrainedBox(maxWidth: sizing.maxContentWidth)`
- 检查是否使用了 `Center` 包裹

### 问题 4: 字体过小
- 检查是否使用了 `sizing.isTablet` 条件判断
- 检查 `Breakpoints.getScaleFactor()` 返回值

## 📊 Responsive Sizing 参考

```dart
final sizing = ResponsiveSizing(context);

// 判断是否为 iPad
if (sizing.isTablet) {
  // iPad 特定逻辑
}

// 获取最大内容宽度（iPad: 600px, Phone: infinity）
sizing.maxContentWidth

// 获取响应式按钮高度（iPad: 最高 72px）
sizing.buttonHeight

// 获取响应式间距
sizing.spacingL  // iPad 上更大
sizing.screenPadding  // iPad 上 1.5 倍

// 获取响应式图标尺寸
sizing.largeIconSize  // iPad: 120, Phone: 80
sizing.splashIconSize  // iPad: 160, Phone: 112
```

## 🚀 快速验证命令

```bash
# 1. 运行自动化测试
flutter test test/ipad_layout_test.dart -v

# 2. 在 iPad 模拟器上运行
flutter run -d "iPad Air"

# 3. 检查 lint 错误
flutter analyze
```

## 📝 添加新页面的 iPad 适配检查清单

1. [ ] 使用 `ResponsiveSizing(context)` 获取响应式尺寸
2. [ ] 使用 `Center + ConstrainedBox(maxWidth: sizing.maxContentWidth)` 限制内容宽度
3. [ ] 使用响应式间距 (`sizing.spacingL` 等) 而非固定值
4. [ ] 使用响应式字体 (`sizing.isTablet ? 18 : 16` 等)
5. [ ] 确保底部按钮有足够的 SafeArea 边距
6. [ ] Row 中的文本使用 `Expanded` 或 `Flexible`
7. [ ] 添加对应的 widget test 到 `test/ipad_layout_test.dart`

---

*最后更新: 2026-01-22*

